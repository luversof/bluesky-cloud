encrypt:
  keyStore:
    location: classpath:/bluesky.jks
    password: blueskyStorePass
    alias: bluesky-Project
    secret: blueskyKeyPass

spring:  cloud:    config:      discovery:        enabled: false